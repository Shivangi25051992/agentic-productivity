# ============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# ============================================================================
# Copy this file to .env.production and fill in actual values
# NEVER commit .env.production to git!
# ============================================================================

# ============================================================================
# ENVIRONMENT
# ============================================================================
ENVIRONMENT=production

# ============================================================================
# OPENAI API KEY (Required)
# ============================================================================
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-REPLACE_WITH_YOUR_ACTUAL_KEY

# ============================================================================
# GOOGLE CLOUD / FIREBASE (Required)
# ============================================================================
# Your Google Cloud Project ID
GOOGLE_CLOUD_PROJECT=your-project-id

# Path to service account JSON file (for local testing)
# In Cloud Run, this is handled automatically
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Firebase Project ID (usually same as GOOGLE_CLOUD_PROJECT)
FIREBASE_PROJECT_ID=your-project-id

# ============================================================================
# CORS (Required for Production)
# ============================================================================
# Comma-separated list of allowed origins (NO SPACES!)
# Include your Firebase Hosting URL and any custom domains
CORS_ORIGINS=https://productivityai-mvp.web.app,https://productivityai-mvp.firebaseapp.com

# ============================================================================
# ADMIN PORTAL (Optional but Recommended)
# ============================================================================
# Admin username
ADMIN_USERNAME=admin

# Admin password (use bcrypt hash for security)
# Generate bcrypt hash: python -c "from bcrypt import hashpw, gensalt; print(hashpw(b'your-password', gensalt()).decode())"
ADMIN_PASSWORD_BCRYPT=$2b$12$REPLACE_WITH_BCRYPT_HASH

# JWT secret for admin tokens (generate random string)
# Generate: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_SECRET_KEY=REPLACE_WITH_RANDOM_SECRET_KEY

# Optional: Comma-separated list of allowed admin IPs
# Leave empty to allow from any IP
ADMIN_IP_WHITELIST=

# ============================================================================
# ENCRYPTION (Optional but Recommended)
# ============================================================================
# Fernet encryption key for sensitive data
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=REPLACE_WITH_GENERATED_KEY

# ============================================================================
# FEATURE FLAGS (Optional)
# ============================================================================
# Enable free tier limits (3 plans/week)
ENABLE_FREE_TIER_LIMITS=true

# Enable parallel meal plan generation (faster)
ENABLE_PARALLEL_GENERATION=true

# Enable analytics and monitoring
ENABLE_ANALYTICS=true

# ============================================================================
# PERFORMANCE TUNING (Optional)
# ============================================================================
# Maximum timeout for LLM API calls (seconds)
MAX_LLM_TIMEOUT=120

# Maximum concurrent LLM API calls (for parallel generation)
MAX_CONCURRENT_LLM_CALLS=7

# ============================================================================
# NOTES
# ============================================================================
# 1. Replace all REPLACE_WITH_* placeholders with actual values
# 2. Never commit this file to git (it's in .gitignore)
# 3. Use strong passwords and secrets
# 4. For Cloud Run deployment, set these as environment variables in GCP Console
# 5. Test locally with: ENVIRONMENT=production python -m uvicorn app.main:app
# 6. Copy this to .env.production: cp env.production.template .env.production


