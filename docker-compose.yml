version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: yuvi-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3
    restart: unless-stopped
    networks:
      - yuvi-network

  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: yuvi-redis-commander
    environment:
      - REDIS_HOSTS=local:redis:6379
    ports:
      - "8081:8081"
    depends_on:
      - redis
    restart: unless-stopped
    networks:
      - yuvi-network

volumes:
  redis_data:
    driver: local

networks:
  yuvi-network:
    driver: bridge

