name: ai_productivity_app
description: Cross-platform AI productivity app (iOS, Android, Web)
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.1

  # Firebase
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.0.3
  firebase_messaging: ^16.0.3
  # firebase_performance: ^0.10.0+7  # 🚀 TODO: Re-enable after fixing version conflicts
  # firebase_crashlytics: ^4.1.3  # 🚀 TODO: Re-enable after fixing version conflicts
  google_sign_in: ^7.2.0
  flutter_local_notifications: ^19.5.0
  timezone: ^0.10.1
  flutter_timezone: ^1.0.8
  # sentry_flutter: ^8.0.0  # 🚀 TODO: Re-enable after fixing version conflicts

  # HTTP & API
  http: ^1.1.2
  dio: ^5.4.0

  # UI Components
  flutter_chat_ui: ^2.9.1
  intl: ^0.20.2
  flutter_svg: ^2.0.9
  fl_chart: ^1.1.1

  # Voice Input
  speech_to_text: ^7.3.0
  permission_handler: ^12.0.1

  # Local Storage
  shared_preferences: ^2.2.2

  # Utilities
  uuid: ^4.3.3
  timeago: ^3.6.0
  image_picker: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/


